{{^JS}}import { CLICommand } from '@micra/cli-router';
import { Context } from '@micra/cli/@types/app/context/types';

{{/JS}}
{{#JS}}exports.{{ NAME.CAMEL }} = { {{/JS}}{{^JS}}export const {{ NAME.CAMEL }}: CLICommand = { {{/JS}}
  command: '{{ NAME.RAW }}',
  description: '',
  middlewares: [
    //
  ],
  arguments: [
    //
  ],
  options: [
    //
  ],
  {{#JS}}async handler(ctx) { {{/JS}}{{^JS}}async handler(ctx: Context) { {{/JS}}
    //
  },
};
