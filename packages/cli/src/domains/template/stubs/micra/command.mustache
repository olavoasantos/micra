import { CLICommand } from '@micra/cli-router';
import { Context } from 'app/context/types';

export const {{ NAME.CAMEL }}: CLICommand = {
  command: '{{ NAME.RAW }}',
  description: '',
  middlewares: [
    //
  ],
  arguments: [
    //
  ],
  options: [
    //
  ],
  async handler(ctx: Context) {
    //
  },
};
