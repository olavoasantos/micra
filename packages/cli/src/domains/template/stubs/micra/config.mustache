const { join } = require('path');

module.exports = {
  app: {
    // name: string;
    // version: string;
    // description: string;
    config: [
      // absolute path to config file
    ],
  },
  providers: [
    // ServiceProviders
  ],
  commands: [
    // CLICommand
  ],
  middlewares: [
    // RouteMiddleware
  ],
  template: {
    // [alias: string]: 'absolute/path/to/template.mustache',
  },
  paths: {
    root: join(process.cwd(), 'src'),
    // [alias: string]: 'absolute/path',
  },
};
